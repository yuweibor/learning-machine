import React, { useEffect, useRef } from 'react';
import './styles.scss';``

const App: React.FC = () => {

  useEffect(() => {
  
  }, []);

  return (
    <div className="game-container">
     
    </div>
  );
};

export default App;